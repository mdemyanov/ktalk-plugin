---
properties:
  - name: Тип контента
    value: [Прочее]
  - name: Фаза
    value: [Pilot]
  - name: Статус
    value: [Draft]
  - name: Audience
    value: [Internal]
---

# Тест-дизайн: CLI-граница (снятие MCP-поверхности плагина, пин версии пакета)

**Capability:** `openspec/specs/cli-only-boundary/spec.md`
**Требование:** [2026-08-31-cli-only-boundary.md](../2026-08-31-cli-only-boundary.md)
**ADR:** `content/00-project/adr/ADR-022-cli-only-boundary.md`
**Архитектура:** `content/40-architecture/2026-08-31-cli-only-boundary.md`

Предмет этого дерева — три изменения из брифа QA-001: снятие MCP-декларации плагина, точный
пин версии пакета, новая проверка состава на возврат `.mcp.json`. Стабы живут в
`scripts/test-onboard.sh` (существующая сьюта онбординга) — тесты 5, 22, 28 изменены под новую
семантику, тесты 32–41 добавлены.

## Покрытие 9 сценариев спеки

| AC ID | `#### Scenario:` спеки | Покрыт чем | Тип |
|---|---|---|---|
| AC-1 | CLI runs without the MCP-only dependency | **Внешний.** Дерево пакета `ktalk-mcp`, extra `fastmcp` в `pyproject.toml` — своя SA/QA-цепочка того репозитория, не эта задача (BA-001 §«Область») | внешний |
| AC-2 | The MCP entry point is launched without the MCP-only dependency | **Внешний**, там же | внешний |
| AC-3 | Contributor test run is unaffected | **Внешний**, там же | внешний |
| AC-4 | Default install completes within an interactive timeout | **Внешний**, там же (замер BA-001 — 23,1 с / 152,8 с — уже приведён с источником и датой, не переизмеряется здесь) | внешний |
| AC-5 | No MCP server is declared | тест 40 (`test-onboard.sh`) — прогоняет `check-plugin-composition.sh` на реальном `.mcp.json` репозитория, ожидает провал и упоминание файла в сообщении | integration (мутационный по построению — файл уже нарушает норму) |
| AC-6 | An operator who previously called an MCP tool directly | тест 41 (`test-onboard.sh`) — присутствие таблицы «retired → CLI», все 3 CLI-эквивалента дословно; фактическое отсутствие `mcp__ktalk__*` в списке инструментов сессии — **платформенное поведение, ручная/e2e проверка** (не автоматизируется в этом репозитории, см. «Не покрывается») | unit (документ) + ручная (сессия) |
| AC-7 | Installed version differs from the pin | тесты 3, 3a (ниже пина — не менялись), 5/32/33 (выше пина — новое), 36 (malformed input), 37/38 (masked failure на compat.json), 27/29 (постусловие, не менялись) | integration |
| AC-8 | The remedy command names the exact version | тесты 22/34/35 (называет пин и в `check --json`, и в аргументах `uv`) | integration |
| AC-9 | The plugin is not released ahead of its pinned package version | **Не покрывается стабом этого дерева** — сетевой шаг релизного пайплайна, имя скрипта и механизм ещё не выбраны (companion-статья, «Форма пред-релизного условия»; бриф DevOps). Вне трёх изменений, названных брифом QA-001 §1, и вне мандата Dev по этой архитектуре (там же, «Brief for Dev» не включает AC-9). Закрывает DevOps как отдельный шаг пайплайна | внешний / N/A для этого стаба |

## Что проверяется автоматически / руками / не проверяется

| Сценарий | Автотест | Руками | Не проверяется |
|---|---|---|---|
| AC-5 | ✅ тест 40 | — | — |
| AC-6 (таблица) | ✅ тест 41 | — | — |
| AC-6 (сессия без инструмента) | — | ✅ владелец/QA-runner проверяет список инструментов сессии после установки плагина | — |
| AC-7 | ✅ тесты 3, 3a, 5, 27, 29, 32, 33, 36, 37, 38 | — | нормализация pre-release/build-суффикса (тест 39 — заглушка на решение Dev) |
| AC-8 | ✅ тесты 22, 34, 35 | — | — |
| AC-9 | — | — | ✅ N/A для этого дерева — DevOps, отдельный пайплайн-шаг |
| AC-1..AC-4 | — | — | ✅ N/A для этого дерева — пакет `ktalk-mcp` |

## Boundary cases

- Версия равна пину ровно (тест 4) — не меняется, `version_eq` обязана вести себя как `version_ge`
  здесь.
- Версия отличается на один патч в обе стороны (тесты 3/3a — ниже; 5 — выше) — симметрия пина,
  не порога.
- `0.9.2` против `0.10.0` — посегментное числовое сравнение, не лексикографическое (тест 3a, не
  менялся, актуально и для равенства).
- **pre-release/build-метаданные** (`0.10.0+local` против пина `0.10.0`) — тест 39, намеренная
  незелёная заглушка: требование не специфицирует норму (companion-статья, раздел «Точка
  правки»), Dev обязан решить и заменить заглушку реальной проверкой.
- Постусловие после ремонта (`finish_install`) — тесты 27/29 (не менялись, версия ниже пина после
  ремонта) плюс симметрия для «выше» покрыта тестом 33 через ветку install/upgrade.

## Error cases (два обязательных класса)

**Malformed/mistyped input** (не отсутствие значения и не выход за диапазон — искажённое
значение от пользователя/окружения):
- Тест 36: `ktalk --version` печатает нераспознаваемую строку (`dev-build`, не semver) — `check`
  обязан явно отказать (`11`), не упасть с необработанной ошибкой и не признать версию тихо
  совместимой. AC-7.
- Для AC-5/AC-6 класс не применим: обе стороны сценария не принимают пользовательский ввод
  (декларация файла инспектируется статически, таблица — статичный документ) — **N/A**.
- Для AC-9 класс не применим в этом дереве (сценарий не реализуется здесь) — **N/A**.

**Masked failure** (система тихо принимает, редиректит или откатывается к дефолту вместо
наблюдаемой ошибки):
- Тест 32: версия НОВЕЕ пина без `allow_update` — до этого изменения такое состояние всегда было
  тихим `0` (порог трактовал «новее» как безусловно ОК); теперь обязана требовать санкцию явно.
  AC-7/Д2 ADR-022.
- Тесты 37/38: `compat.json` без ключа пина (вовсе, либо только старый ключ
  `ktalk_mcp_min_version` после переименования) — обязан явно отказать (`20`), не откатиться к
  «любая версия подходит». Мутационная демонстрация (не в составе стаба, приложена в отчёте
  QA-author): фиктивный дефолт `min_version() → 0.0.0` при отсутствии ключа переводит exit code
  20→0 — ассерт ловит эту деградацию. AC-7.
- Тест 40: `.mcp.json` продолжает декларировать `ktalk` как MCP-сервер, а
  `check-plugin-composition.sh` этого не замечает — операторская сессия молча получает
  MCP-инструменты контура без единого провалившегося гейта. Красный уже сегодня без всякой
  мутации: файл реально нарушает норму, проверки для него ещё нет. AC-5.
- Для AC-6 (таблица) класс не применим отдельным тестом — риск «тихого возврата» инструмента
  закрывается транзитивно тестом 40 (декларация — единственный канал, которым инструмент вообще
  становится доступен сессии) — **N/A как отдельная строка, закрыт AC-5**.
- Для AC-9 — N/A в этом дереве (сценарий не реализуется здесь).

## Не покрывается (out of scope этого дерева)

- **AC-1..AC-4** (extra `fastmcp`, холодный старт, dev-группа пакета) — репозиторий `ktalk-mcp`,
  собственный QA-author того дерева. BA-001 §«Область» и companion-статья §«Context» оговаривают
  это явно.
- **AC-9** (пред-релизный гейт «пин уже опубликован») — отдельный шаг релизного пайплайна,
  сетевой, не pre-commit; имя скрипта не выбрано (companion-статья, «Форма пред-релизного
  условия»; «Brief for DevOps»). Не входит в три изменения брифа QA-001 §1 и не входит в «Brief
  for Dev» той же архитектурной статьи — закрывает DevOps отдельной задачей, не Dev этой волны.
- **AC-6, платформенная часть** (реальное отсутствие `mcp__ktalk__*` в списке инструментов
  операторской сессии Claude Code) — поведение хоста, не наблюдаемое статическим тестом в этом
  репозитории (архитектура, «Test-pyramid recommendation»). Ручная/QA-runner проверка после
  установки плагина без `.mcp.json`.
- **Синтаксис пре-релиз/билд-метаданных в `version_eq`** — требование не специфицирует; тест 39
  зафиксирован как решение, которое обязан принять Dev, не как готовый ассерт.

## Дев-хинты (для расшифровки красных стабов)

| Тест(ы) | Компонент | Хинт |
|---|---|---|
| 5, 32, 33 | `ktalk-onboard.sh: cmd_check`/`cmd_install` | `version_ge` → `version_eq`; «новее» перестаёт быть ранним `ok` — сначала санкция |
| 22, 34, 35 | `remedy_cmd_text/array` | обе ветки (install/update) зовут одну и ту же команду `uv tool install ktalk-mcp==<пин>`, не `upgrade`; пин виден и в `check --json`, не только при установке |
| 28 | `finish_install` | verb-агностичное постусловие: перечитывает версию, не доверяет коду возврата и тексту "Nothing to upgrade"/"Already installed" |
| 36 | `installed_version`/`cmd_check` | нераспознанная строка версии = пустая строка = несовпадение с пином, код 11 |
| 37, 38 | `min_version`→`pin_version`, `compat.json` | читает **новый** ключ `ktalk_mcp_version`; отсутствие ключа (в любой форме, включая только старый ключ) — `E_INTERNAL=20`, без дефолта |
| 39 | `version_eq` | Dev решает и кодирует норму для pre-release/build-суффикса, обосновывает в комментарии |
| 40 | `check-plugin-composition.sh` | новая проверка: `.mcp.json` либо отсутствует, либо не содержит `ktalk` в `mcpServers`; сообщение называет файл |
| 41 | README.md/`references/onboarding.md` | таблица «Retired MCP → CLI» с тремя CLI-командами дословно; первые два ряда — по роли, без запрещённых литералов (см. `check-plugin-composition.sh`) |

## Известное ограничение

`scripts/test-onboard.sh` читает реальный `compat.json` репозитория для большинства тестов пина
(меняя только вывод стаба `ktalk --version`) — это достаточно для симметрии «выше/ниже» и для
проверки текста команды ремонта. Для сценария «ключ пина отсутствует» тесты 37/38 зеркалят
дерево (`scripts/ktalk-onboard.sh` копируется, `compat.json` подставляется синтетический) —
приём не требует правки реального `compat.json` и не создаёт по нему конфликт с DEV-002.
