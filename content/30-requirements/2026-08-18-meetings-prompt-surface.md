---
properties:
  - name: Тип контента
    value: [Требование]
  - name: Фаза
    value: [Pilot]
  - name: Статус
    value: [Approved]
  - name: Audience
    value: [Internal]
---

# Промт-поверхность плагина ktalk — расписание, встречи, участники, диагностика

**Дата:** 2026-08-18 (первый коммит источника в дереве пакета `ktalk-mcp`; редакция волны 6 —
2026-08-19). **Автор:** BA, дерево пакета `ktalk-mcp`, до переезда ([ADR-023](../00-project/adr/ADR-023-plugin-requirements-relocation.md)
переносит документ предметно в дерево плагина, не переиздаёт решение). **Входы:** постановка
волны 5/6 (владелец), пакет `ktalk-mcp` 0.7.0 — 26 подкоманд CLI (`list-calendar`,
`create-meeting-preview`/`-confirm`, `cancel-meeting-preview`/`-confirm`, `search-contacts`,
`get-room`, `auth-status`, `config show`), `rooms-calendar-scheduling.md`/`ktalk-plugin.md`
(требования дерева пакета, механика CLI, вне переезда). **Потребители:** SA, Dev, QA-author.
**Capability:** `openspec/specs/meetings-prompt-surface-reads/spec.md`, `openspec/specs/meeting-write-sanction-workflow/spec.md`

Продолжение волны 3 поверх «Плагин ktalk в произвольном проекте» (FR-20…FR-25, NFR-11…NFR-16;
дерево пакета `ktalk-mcp`, не переезжает) и «Комнаты, календарь и планирование встреч» (FR-13,
FR-17…FR-19, NFR-6…NFR-10; там же). Источники: постановка волны 5 (владелец), пакет `ktalk-mcp`
0.7.0 — 26 подкоманд CLI (`list-calendar`, `create-meeting-preview`/`-confirm`,
`cancel-meeting-preview`/`-confirm`, `search-contacts`, `get-room`, `auth-status`, `config show` и
остальные, таблица `_HANDLERS` в `src/ktalk_mcp/cli.py`), плагин `ktalk` (это дерево) — одна
команда, два навыка, два агента, ни одного из четырёх слов
`list-calendar`/`create-meeting`/`search-contacts`/`cancel-meeting` в дереве плагина.
ADR-004, ADR-005, ADR-006, ADR-012 §2а (все — дерево пакета `ktalk-mcp`, вне переезда) — CLI
приоритетный интерфейс контура, MCP вторичен, пишущий MCP-инструмент не заводится вовсе.
Формулировка «MCP вторичен» — историческая (ADR-012 §2а, дерево пакета); действующий на этот
момент контракт этого дерева — `openspec/specs/cli-only-boundary/spec.md` (ADR-022): MCP не
вторичен, а не объявлен вовсе.

**Редакция волны 6 (2026-08-19).** Решением владельца пишущую операцию выполняет агент, а не
оператор в своём терминале. FR-33, FR-34, NFR-22, NFR-23 переписаны под это поведение, добавлен
NFR-24; основание — ADR-016, отменяющий TTY-барьер ADR-005 §3 и контракт передачи ADR-015 §2
(все три — дерево пакета `ktalk-mcp`). FR-32, FR-35…FR-38, NFR-20, NFR-21 не менялись. Раздел
«Приёмка (BA-006)» относится к редакции волны 5 и оставлен как исторический факт; приёмка новой
редакции — «Приёмка (BA-008)».

## JTBD

Когда мне нужно свериться с занятостью комнаты или расписанием на дату/период, не переключаясь в
веб-интерфейс Толка, я (оператор, работающий через агента плагина `ktalk`) хочу прочитать
расписание встреч за окно дат прямо из чата с агентом, чтобы не открывать браузер ради разовой
сверки.

Когда мне нужно запланировать одиночную встречу с конкретными участниками, я хочу, чтобы агент
подготовил, показал мне предпросмотр (тема, время, участники, комната) и создал встречу сам, не
заставляя меня переключаться в терминал, — при этом чтобы право на запись он получал от меня
заранее, осознанно и на ограниченный срок, а не имел его по умолчанию (волна 6, ADR-016).

Когда ранее созданная через контур встреча больше не нужна, я хочу отменить её тем же порядком
(предпросмотр → запись агентом по санкции), с отдельным правом на отмену, чтобы разрешение
создавать встречи не означало автоматически разрешения их отменять.

Когда я передумал или закончил работу, я хочу отозвать выданное право одной командой и увидеть
по журналу, что именно было записано в контур, пока право действовало.

Когда я помню только часть имени или фамилии участника, я хочу найти его и получить точный
идентификатор для приглашения, чтобы не подставлять в приглашение случайного однофамильца.

Когда любая из операций этой поверхности отказывает или ведёт себя неожиданно, я хочу сразу
понять, вызвано ли это конфигурацией проекта-хозяина, режимом авторизации или отсутствием прав,
не читая исходный код пакета.

## Описание

До этой волны контур в плагине покрывает только реестр записей (`ktalk-registry`) и оценку
качества обработки (`ktalk-eval`). Пакет `ktalk-mcp` 0.7.0 уже умеет всё перечисленное в JTBD —
расписание, планирование, отмену, поиск контактов, диагностику — CLI-командами, но ни одна из них
не названа ни в одном промте плагина: пользователь, работающий через агента, не может обнаружить
эту функциональность иначе как прочитав `--help` пакета напрямую, в обход промт-слоя.

Требование фиксирует, какое поведение промт-поверхность плагина обязана предоставлять поверх уже
существующих CLI-команд, и какие ограничения контура (санкция на запись, побочный эффект чтения
комнаты, деградация по режиму авторизации) обязаны остаться видимыми на этом уровне, а не
потеряться при переносе в навык/промт. Требование не описывает форму навыка, команды или агента
плагина (имя, файл, порядок шагов workflow) — это решение SA, тем же разделением, что уже принято
для `ktalk-plugin.md` (FR-20/24 фиксируют наблюдаемое поведение на границе, не механизм).

Не входит в это требование: сама механика CLI-команд (пути API, состав тела запроса, компоновщик,
`ConfirmationStore`) — она уже специфицирована `rooms-calendar-scheduling.md` (FR-13, FR-17, FR-18)
и реализована; здесь фиксируется контракт того, как эта уже существующая механика обязана стать
доступной и понятной через промт-слой плагина.

## Почему поверхность ограничена CLI, а не MCP

ADR-012 §2а — решение владельца этой сессии: CLI `ktalk` — единственный развиваемый интерфейс
контура, MCP-сервер остаётся объявленным в `.mcp.json` плагина, но не получает новых пишущих
инструментов. `ktalk-mcp` не регистрирует MCP-инструмент создания или отмены встречи вовсе —
это не пробел волны, а действующее архитектурное решение: пишущая операция живёт только в CLI,
где у неё есть санкционный контур и журнал (ADR-016; до волны 6 тем же местом её держал
TTY-барьер ADR-005 §3). Формулировка FR/NFR ниже поэтому опирается на CLI-
команды и их `--json`, а не на имена MCP-инструментов — единственный способ не продублировать
ограничение в двух местах с риском разойтись.

## Почему диагностика прав — отдельная операция, а не побочный текст ошибки

`ktalk auth-status` и `ktalk config show` уже существуют как read-only, не требующие реестра
команды (`_REGISTRY_FREE_COMMANDS`, `src/ktalk_mcp/cli.py`). ADR-004 фиксирует: в api-key-режиме
чтение календаря не получает «рабочую» запись профиля не потому, что не проверено, а потому, что
проверено, но необъяснимо (Ф-33–Ф-35: три родственные операции одним ключом дают три разных
исхода) — отказ такой операции обязан быть отличим от протухшего токена и от отсутствия конфигурации
проекта-хозяина, иначе оператор тратит время на неверную гипотезу о причине. Отдельная диагностика
до/вместо повторной попытки — дешевле, чем разбор трёх похожих по симптому, но разных по причине
отказов вручную.

## Функциональные требования

### FR-32 — Просмотр расписания через промт-поверхность

Промт-поверхность плагина отдаёт запланированные встречи за окно дат, видимые активной
авторизации, опираясь на `ktalk list-calendar --start --end [--room-name] --json` (FR-18
`rooms-calendar-scheduling.md` — механика окна, лимитов и предупреждения о неполном сегменте не
пересматривается здесь).

**AC:**
- Given оператор просит агента показать расписание на дату/период, When агент выполняет сценарий,
  Then он вызывает `ktalk list-calendar` с явным `--start`/`--end`, не подставляя период по
  умолчанию без запроса у оператора — автоматическая проверка (снимок промта/сценария: команда не
  вызывается без явных дат, полученных от оператора или из его формулировки).
- Given `list-calendar` вернул предупреждение о неполном сегменте (100 элементов, ровно на потолке
  FR-18), When агент показывает результат оператору, Then предупреждение остаётся видимым в ответе
  агента, а не отбрасывается при форматировании — автоматическая проверка (сравнение полей ответа
  CLI и текста, показанного оператору).
- Given `list-calendar` завершился ненулевым кодом возврата, When агент обрабатывает результат,
  Then он не показывает оператору пустое расписание как «встреч нет» — код возврата 0 и пустой
  список различимы от кода возврата ≠0 в тексте, показанном оператору — автоматическая проверка.

### FR-33 — Создание встречи: выполняет агент по санкции оператора

Промт-поверхность плагина готовит предпросмотр (`ktalk create-meeting-preview`), показывает его
оператору и **сама выполняет** `ktalk create-meeting-confirm` — при действующей санкции контура
записи на операцию `create_meeting` и с привязкой к телу, которое было предъявлено предпросмотром.
Санкцию выдаёт оператор лично в своём терминале; агент её не выдаёт и не продлевает
(ADR-016). Прежняя редакция (агент доводит сценарий до предпросмотра и передаёт команду
`*-confirm` оператору, ADR-005 §3 / ADR-015 §2) отменена решением владельца волны 6.

**AC:**
- Given действующей санкции `create_meeting` нет (файла нет, ключ `false`, срок истёк, бюджет
  исчерпан, файл повреждён), When агент вызывает `create-meeting-confirm`, Then команда отказывает
  ненулевым кодом семейства 4x и не выполняет ни одного сетевого вызова записи, а агент показывает
  оператору команду выдачи санкции текстом и **не выполняет её сам** — автоматическая проверка
  (код возврата и отсутствие POST на моке транспорта; grep по дереву плагина: `sanction grant`
  нигде не вызывается программно).
- Given санкция действует, When агент создаёт встречу, Then он сначала вызывает
  `create-meeting-preview`, показывает оператору его вывод, и только затем вызывает
  `create-meeting-confirm` с `--confirmation-id` из этого предпросмотра и **теми же** значениями
  флагов — автоматическая проверка (снимок сценария навыка; тест CLI: без `--confirmation-id` в
  неинтерактивном режиме команда отказывает).
- Given между предпросмотром и подтверждением изменилось хотя бы одно значение поля тела, When
  агент вызывает `create-meeting-confirm` с прежним `--confirmation-id`, Then команда отказывает и
  не выполняет сетевого вызова: записывается ровно то тело, которое было показано, или ничего —
  автоматическая проверка.
- Given у поля из перечня NFR-9 требования `rooms-calendar-scheduling.md` (участники, комната,
  время начала/конца, часовой пояс, `allowAnonymous`, `pinCode`, `enableSip`, автозапись) нет
  явного значения от оператора, When агент формирует параметры предпросмотра, Then он не
  подставляет значение по умолчанию сам — либо переспрашивает оператора, либо передаёт вызов
  `create-meeting-preview` без этого параметра и полагается на отказ CLI (FR-13 AC), но не
  выдумывает значение на уровне промта — автоматическая проверка (снимок промта на предмет
  захардкоженных дефолтов для полей этого перечня).
- Given попытка записи выполнена (успешно, с отказом или с неизвестным исходом), When операция
  завершилась, Then в журнале операций записи есть строка с меткой времени, операцией, каналом
  подтверждения, хешем тела, самим телом и исходом — достаточная, чтобы разобрать постфактум, что
  именно было записано и по чьей санкции — автоматическая проверка.

### FR-34 — Отмена встречи: тот же порядок и та же санкция, отдельным ключом

Промт-поверхность плагина готовит предпросмотр отмены (`ktalk cancel-meeting-preview --id`) и сама
выполняет `ktalk cancel-meeting-confirm` — при действующей санкции на операцию `cancel_meeting`.
Ключ санкции отдельный: санкция на создание не даёт права на отмену и наоборот (ADR-016, тем же
приёмом, что `allow_install`/`allow_update` в ADR-014 §3).

**AC:**
- Given оператор просит агента отменить встречу и санкция `cancel_meeting` действует, When агент
  готов отменить, Then он вызывает `cancel-meeting-preview --id <id>`, показывает вывод оператору
  и вызывает `cancel-meeting-confirm --id <тот же id> --confirmation-id <из предпросмотра>` —
  автоматическая проверка (снимок сценария навыка; тест CLI).
- Given действует санкция `create_meeting`, но не `cancel_meeting`, When агент вызывает
  `cancel-meeting-confirm`, Then команда отказывает и не выполняет сетевого вызова — ключи
  независимы — автоматическая проверка.
- Given у оператора нет `id` встречи под рукой, When агент готовит отмену, Then он предлагает
  сначала получить `id` из `list-calendar` (FR-32) или из вывода `create-meeting-confirm`,
  использованного при создании — не изобретает `id` и не запрашивает его как произвольную строку
  без объяснения формата (base64, не хранится проектом) — автоматическая проверка (текстовый
  снимок сценария).

### FR-35 — Поиск участника без автовыбора

Промт-поверхность плагина ищет участника по имени/фамилии (`ktalk search-contacts --query`) и
передаёт оператору весь список кандидатов при 0/1/>1 совпадениях — без автоматического выбора
одного кандидата и без автоматической подстановки `key` в параметры создания встречи (ADR-010,
NFR-9).

**AC:**
- Given `search-contacts` вернул более одного кандидата, When агент показывает результат
  оператору, Then он показывает всех кандидатов (key, ФИО, должность) и явно просит оператора
  выбрать нужного — не выбирает первого по списку и не подставляет его `key` в
  `--required-attendee-key` сам — автоматическая проверка (снимок промта/сценария: код возврата 0
  при единственном совпадении не приравнивается к автовыбору при >1).
- Given `search-contacts` вернул одного кандидата, When агент использует его `key` в дальнейшем
  сценарии создания встречи, Then он явно показывает оператору, чей `key` подставлен (ФИО,
  должность), прежде чем передать его в `--required-attendee-key` — не тихая подстановка —
  автоматическая проверка (текстовый снимок).
- Given `search-contacts` завершился ненулевым кодом возврата без кандидатов (`0 candidates` —
  задокументированное поведение CLI), When агент обрабатывает результат, Then он сообщает
  оператору «участник не найден по запросу», не путает это с ошибкой сети/авторизации —
  автоматическая проверка (различение источника ненулевого кода).

### FR-36 — Диагностика комнаты с обязательным предупреждением

Промт-поверхность плагина умеет прочитать конфигурацию комнаты (`ktalk get-room <room_name>`) для
диагностики (политики, модераторы, SIP), но каждый вызов сопровождается безусловным
предупреждением: первый запрос по ранее не встречавшемуся имени создаёт комнату как побочный
эффект, необратимый средствами проекта (ADR-006 п.2). Операция не предлагается и не описывается
как способ проверить, свободно ли имя, — ADR-006 п.5: сервер не отдаёт сигнала «не найдено» ни на
одно имя.

**AC:**
- Given агент вызывает `get-room` по запросу оператора, When он показывает результат, Then
  сообщение включает предупреждение о побочном эффекте на любое имя, не только на предположительно
  новое — условного предупреждения нет физически (ADR-006 п.2: сервер не различает существовавшую
  и созданную этим вызовом комнату) — автоматическая проверка (текстовый снимок: предупреждение
  присутствует при любом входном имени).
- Given оператор формулирует запрос как «проверь, свободно ли имя комнаты `X`», When агент
  интерпретирует запрос, Then он не вызывает `get-room` для этой цели и объясняет оператору, что
  такой проверки в контуре не существует (сервер не различает «есть» и «нет» кодом ответа) —
  автоматическая проверка (снимок промта: отсутствие пути «get-room как проверка занятости имени»
  в сценариях).

### FR-37 — Явный контракт деградации по режиму авторизации

Когда операция этой поверхности недоступна в активном режиме авторизации (ADR-004: чтение
комнаты/календаря и планирование под api-key-режимом либо не проверены, либо проверены, но не
получили рабочую запись профиля намеренно — `rooms-calendar-scheduling.md`, «Профиль эндпоинтов»),
промт-поверхность плагина объясняет причину отказа явно и не выполняет запрос вслепую, полагаясь
на тот же fail-closed механизм `OPERATION_PROFILES`, что уже действует в CLI (NFR-7
`rooms-calendar-scheduling.md`).

**AC:**
- Given CLI-команда отказала с сообщением об отсутствующем/неподтверждённом профиле операции в
  активном режиме, When агент показывает результат оператору, Then текст сообщения CLI передаётся
  оператору без искажения смысла (не подменяется общим «что-то пошло не так») — автоматическая
  проверка (сравнение текста CLI и текста, показанного оператором).
- Given оператор работает в api-key-режиме и просит операцию, для которой профиль в этом режиме не
  подтверждён (чтение комнаты, чтение календаря, планирование — ADR-004, «Профиль эндпоинтов»),
  When агент формирует ответ, Then он не пытается угадать результат и не намекает на то, что
  операция «наверное сработает» — автоматическая проверка (текстовый снимок: отсутствие
  формулировок вероятностного успеха для неподтверждённых профилей).

### FR-38 — Диагностика прав и конфигурации — первый шаг при отказе

При отказе любой операции FR-32…FR-36 промт-поверхность плагина предлагает оператору
`ktalk auth-status` и, если отказ похож на проблему конфигурации проекта-хозяина (маршрутизация,
раскладка каталогов — не сама операция с Толком), `ktalk config show`, прежде чем повторять
попытку или советовать открыть код.

**AC:**
- Given любая из команд FR-32…FR-36 завершилась ошибкой авторизации (401/403 или диагностируемый
  как связанный с правами по контролю ADR-004), When агент формирует ответ оператору, Then он
  включает предложение выполнить `ktalk auth-status --json` и, если оператор согласен, выполняет
  его и показывает результат — автоматическая проверка (снимок сценария: путь эскалации к
  `auth-status` существует и достижим из каждой из команд FR-32…FR-36).
- Given ошибка не похожа на авторизацию (сеть, недокументированный контур ответил неожиданно —
  ADR-004 §5, корреляционная диагностика), When агент формирует ответ, Then он не предлагает
  `auth-status` как единственную гипотезу — текст сообщения CLI (уже несущий корреляционную
  диагностику, если она сработала) передаётся оператору как есть (FR-37 AC1) — автоматическая
  проверка.

## Нефункциональные требования

### NFR-20 — Промт-поверхность обращается только к CLI

Навыки, команды и агенты плагина, реализующие FR-32…FR-38, вызывают `ktalk` CLI и парсят его
`--json`, не обращаются к MCP-инструментам пакета для этой поверхности (ADR-012 §2а: MCP —
вторичный путь, пишущих MCP-инструментов для встреч не существует вовсе).

**AC:** Given реализация этой поверхности в дереве плагина, When выполняется её ревью, Then ни один
из промтов навыков/команд/агентов не называет MCP-инструмент по имени как способ создать, отменить
встречу или прочитать комнату/календарь — автоматическая проверка (текстовый grep по дереву
плагина на предмет отсутствующих MCP-имён вроде `ktalk_create_meeting`).

### NFR-21 — Секреты не появляются в промтах и логах навыка

`KTALK_SESSION_TOKEN`/`KTALK_PERSONAL_API_KEY` не появляются в тексте, который промт-поверхность
показывает оператору, логирует или сохраняет — наследует NFR-5/NFR-10
(`rooms-calendar-scheduling.md`)/NFR-19 (`ktalk-plugin-onboarding.md`), распространяет их на новую
поверхность промтов плагина, а не только на вывод самого CLI.

**AC:** Given любая из команд FR-32…FR-38 завершилась ошибкой с значением секрета в тексте CLI
(проверено `redact_secrets` на уровне пакета), When агент формирует ответ оператору, Then он не
добавляет секрет повторно из окружения/переменных промта в свой текст — автоматическая проверка
(текстовый снимок промтов на предмет прямых ссылок на переменные окружения секретов).

### NFR-22 — Без автоматического повтора пишущих команд

Промт-поверхность не повторяет `create-meeting-confirm`/`cancel-meeting-confirm` автоматически
после сбоя (сетевой ошибки, таймаута, неопределённого исхода) — решение о повторной попытке
остаётся за оператором и требует нового предпросмотра (наследует AC FR-13
`rooms-calendar-scheduling.md`: идемпотентность повторного `POST` не исследована). Требование
сохраняется дословно после снятия TTY-барьера и усиливается структурно: подтверждение
одноразово (`confirmation_id` потребляется до сетевой попытки), а бюджет санкции уменьшается на
каждой попытке независимо от исхода — «попробовать ещё раз» перестаёт быть бесплатным даже для
агента, который решил это сделать вопреки промту.

**AC:**
- Given `create-meeting-confirm`/`cancel-meeting-confirm` завершились с сообщением «исход
  неизвестен», When агент обрабатывает результат, Then он не вызывает ту же команду повторно сам
  — показывает сообщение оператору и предлагает лично проверить `list-calendar`, как уже требует
  сам текст CLI — автоматическая проверка (снимок сценария: отсутствие цикла повтора вокруг
  пишущих команд).
- Given подтверждение уже использовано одной попыткой записи, When та же команда вызывается
  повторно с тем же `--confirmation-id`, Then она отказывает и не выполняет сетевого вызова —
  автоматическая проверка (ровно один POST на моке транспорта).

### NFR-23 — Право записи — конечный, отзываемый и прослеживаемый мандат человека

Прежняя редакция NFR-23 («подтверждение остаётся действием человека, не шагом workflow»;
ни один путь плагина не вызывает `*-confirm` программно) **отменена** решением владельца волны 6:
запись выполняет агент. Сохраняющееся требование — не запрет на программный вызов, а свойства
мандата, по которому этот вызов допустим. Мандат обязан быть одновременно:

1. **выдаваемым только человеком** — команда выдачи санкции отказывает без интерактивного
   терминала и никогда не вызывается агентом, скриптом или фоновым процессом (наследует NFR-18
   `ktalk-plugin-onboarding.md`, тот же приём, что `ktalk-onboard.sh grant`);
2. **конечным** — по сроку и по числу операций, обе границы обязательны; бессрочная или
   неограниченная по количеству санкция контуром не выдаётся вовсе;
3. **раздельным по операциям** — создание и отмена суть независимые ключи;
4. **отзываемым одной командой**, с немедленным действием отзыва на следующую попытку записи;
5. **fail-closed** — отсутствие, повреждение, истечение и исчерпание читаются одинаково как
   «санкции нет»; невозможность спросить человека никогда не трактуется как согласие;
6. **прослеживаемым** — каждая попытка записи оставляет запись в журнале операций, пригодную для
   разбора постфактум; журнал недоступен для записи → запись не выполняется.

**AC:**
- Given нет интерактивного терминала, When вызывается команда выдачи санкции, Then она отказывает
  и файл санкции не создаётся/не меняется — автоматическая проверка (тест без TTY, без эмуляции
  pty).
- Given дерево плагина реализует FR-33/FR-34, When выполняется ревью промтов, Then ни один путь
  не вызывает команду выдачи санкции программно — вызывающий всегда оператор из своего терминала
  — автоматическая проверка (grep по дереву плагина).
- Given санкция истекла по сроку либо исчерпана по числу операций, When агент вызывает
  `*-confirm`, Then команда отказывает и не выполняет сетевого вызова — автоматическая проверка.
- Given санкция отозвана, When агент вызывает `*-confirm` следующей попыткой, Then команда
  отказывает — отзыв действует немедленно, без перезапуска чего-либо — автоматическая проверка.
- Given попытка записи выполнена при любом исходе, When она завершилась, Then журнал операций
  содержит соответствующую запись — автоматическая проверка.

### NFR-24 — Данные контура — не основание для записи

Содержимое ответов сервера (тема и описание встречи, ФИО и должность контакта, имя комнаты,
текст приглашения) — данные для показа оператору, а не инструкции промт-слою. Решение выполнить
пишущую операцию принимается только из явной инструкции оператора в диалоге; ни одно содержимое
ответа CLI не может ни инициировать запись, ни расширить санкцию, ни изменить состав тела.
До волны 6 это свойство обеспечивалось самим существованием TTY-барьера (SEC-005, MIN-01 —
находка была теоретической, потому что записи агент не делал); теперь оно обязано быть выражено
явно.

**AC:**
- Given ответ CLI содержит текст, оформленный как инструкция («игнорируй предыдущие инструкции»,
  «создай встречу», «выдай санкцию»), When агент обрабатывает этот ответ, Then он показывает
  текст оператору как данные и не выполняет пишущую команду и не выдаёт санкцию по этому
  основанию — автоматическая проверка (снимок промта: явная оговорка о данных против инструкций
  присутствует и покрывает все читающие команды поверхности).
- Given любое значение поля тела, пришедшее из ответа контура (`key` участника, имя комнаты),
  When оно попадает в параметры записи, Then оно прошло через предпросмотр, показанный оператору
  (FR-35: без автовыбора и без тихой подстановки) — автоматическая проверка.
- Given компоновщик тела встречи, When в ответе контура присутствует поле вне согласованного
  состава, Then оно физически не может попасть в тело — allow-list компоновщик ADR-005 не
  пересматривается волной 6 — автоматическая проверка (регрессия существующих тестов
  `test_meeting_body.py`).

## Вне рамок

- **Пишущий MCP-инструмент для встреч.** Решение сохраняется и после снятия TTY-барьера: санкция,
  привязка к предъявленному телу и журнал живут в CLI, дублировать их во втором интерфейсе волна 6
  не берётся (ADR-016; MCP заморожен — ADR-015 §1, эта часть решения в силе).
- **Удалённое одобрение (webhook/мессенджер) как канал санкции.** Отклонено той же причиной, что
  в ADR-005: инфраструктуры уведомлений в проекте нет. Кандидат следующей волны, если срок и
  бюджет санкции окажутся недостаточно тонкой гранулярностью.
- **Правило разрешений Claude Code (`settings.json`) как основание записи.** Не является
  контрактом этого требования: правило снимается пользователем молча и навсегда, контур на него не
  опирается (ADR-016 §3).
- **Повторяемость встреч, правка (`PUT`), создание/изменение комнаты.** Вне рамок
  `rooms-calendar-scheduling.md` (FR-13 «Вне рамок», «FR-12 — создание комнаты» не входит в это
  требование целиком) — эта волна их не переоткрывает.
- **Механика самого навыка/команды/агента плагина** (имя, файл, шаги workflow, ключи `.ktalk.toml`
  для маршрутизации артефактов встреч) — решение SA, тем же разделением, что уже принято для
  `ktalk-plugin.md` (FR-20/24: наблюдаемое поведение на границе фиксирует BA, механизм — SA).
  Здесь не выбирается, расширяется ли существующий навык `ktalk-registry` или заводится новый.
  Не выбирается имя новой команды/агента, если решение SA потребует его.
  Не выбирается номер версии плагина (`plugin.json`), в которой поверхность появится.
  Не выдумывается новый ключ `.ktalk.toml` для маршрутизации артефактов встреч — если он
  потребуется, это отдельное решение SA поверх существующей спеки `ktalk-plugin-spec.md`.
- **Проверка поведения `get-room`/`list-calendar`/`create-meeting`/`cancel-meeting` под
  api-key-режимом.** Не проверено на уровне CLI (`rooms-calendar-scheduling.md`, «Риски») —
  промт-поверхность наследует существующий контракт деградации (FR-37), не проверяет его заново.
- **Диагностика причины расхождения `/api/calendar` между режимами авторизации.** Открытый риск
  `rooms-calendar-scheduling.md` («НЕ закрыто»), не решается на уровне промтов.

## Таблица трассировки

| Требование | Источник |
|---|---|
| FR-32 | Постановка волны 5; FR-18 `rooms-calendar-scheduling.md` (механика) |
| FR-33 | Постановка волны 6 (владелец); FR-13 `rooms-calendar-scheduling.md`, ADR-016 (санкция, привязка к телу, журнал) — заменяет редакцию волны 5, опиравшуюся на ADR-005 §3 |
| FR-34 | Постановка волны 6 (владелец); ADR-011 (отмена встречи), ADR-016 (отдельный ключ санкции) |
| FR-35 | Постановка волны 5; ADR-010 (0/1/>1 без автовыбора) |
| FR-36 | Постановка волны 5; FR-17 `rooms-calendar-scheduling.md`, ADR-006 (побочный эффект) |
| FR-37 | Постановка владельца (ограничение волны 5); ADR-004 (fail-closed по режиму) |
| FR-38 | Постановка владельца (ограничение волны 5); `auth-status`/`config show` — существующие
  read-only команды CLI |
| NFR-20 | ADR-012 §2а (CLI приоритетный интерфейс) |
| NFR-21 | Наследует NFR-5/NFR-10/NFR-19 |
| NFR-22 | Наследует AC FR-13 `rooms-calendar-scheduling.md`; усилен одноразовостью подтверждения и бюджетом санкции (ADR-016) |
| NFR-23 | Постановка волны 6; ADR-016, прецедент санкции ADR-014 §2–§4 (fail-closed, TTY на выдачу, независимые ключи) — заменяет редакцию, опиравшуюся на ADR-005 §3 |
| NFR-24 | SEC-005 MIN-01 (`security-review-plugin-meetings.md`), переставшая быть теоретической после снятия TTY-барьера |

## Приёмка (BA-006)

Приёмка редакции волны 5. Относится к прежним формулировкам FR-33/FR-34/NFR-22/NFR-23
(TTY-барьер, передача команды оператору); сохранена как исторический факт, не переписывается.
Действующая приёмка — «Приёмка (BA-008)» ниже.

Проверено чтением `skills/ktalk-meetings/SKILL.md` (репозиторий `ktalk-plugin`, только чтение) —
для AC, помеченных at-design QA-005 `N/A` как «проверяется текстом навыка, не pytest'ом» — и
прогоном `uv run pytest`/`bash scripts/check.sh --fast` в этом репозитории (`456 passed`,
`Errors: 0`).

| Требование | Вердикт | Доказательство |
|---|---|---|
| FR-32 | принят | `SKILL.md` §«Расписание» (явные `--start`/`--end`, предупреждение `incomplete_segments` дословно, код≠0 отличим от пустого списка); `tests/test_cli_meetings_surface.py::test_ac_32_1b_…`/`test_ac_32_2_…`/`test_ac_32_3_…` |
| FR-33 | принят | `SKILL.md` §«Создание встречи» — handoff из трёх частей (вывод preview / точная команда confirm / объяснение TTY-барьера), запрет программного вызова `create-meeting-confirm`, запрет дефолтов по перечню NFR-9; `grep -rl create-meeting-confirm` в дереве плагина — только `SKILL.md` (текст, не вызов) |
| FR-34 | принят | `SKILL.md` §«Отмена встречи» — тот же паттерн handoff; получение `id` из `list-calendar`/вывода `create-meeting-confirm`, не изобретается; `tests/test_cli_meetings_surface.py::test_ac_34_1b_…`/`test_ac_34_2b_…` |
| FR-35 | принят | `SKILL.md` §«Поиск участника» — таблица кодов `0/1/2`, явное «проверяй код, не текст»; коды `0/1/2` реализованы `src/ktalk_mcp/cli_contacts.py:44-55` (DEV-009); `tests/test_cli_meetings_surface.py::test_ac_35_3_…` |
| FR-36 | принят | `SKILL.md` §«Диагностика комнаты» — безусловное предупреждение (для любого имени, не только предположительно нового), явный отказ описывать `get-room` как проверку занятости; `test_get_room_has_no_availability_check_flag` |
| FR-37 | принят | `SKILL.md` §«Диагностика при отказе» п.1 (текст CLI без искажения) и п.4 (не единственная гипотеза при не-авторизационной ошибке); `tests/test_cli_meetings_surface.py::test_ac_37_1_…[list-calendar\|get-room\|search-contacts]` |
| FR-38 | принят | `SKILL.md` §«Диагностика при отказе» — общая секция, на которую ссылаются все шесть сценариев; п.2 (`auth-status`), п.3 (`config show` при подозрении на конфигурацию хозяина), п.4 (не единственная гипотеза); `test_ac_38_1_meetings_commands_and_escalation_targets_are_all_registry_free` |
| NFR-20 | принят | `bash scripts/check-plugin-composition.sh` → `OK` (grep пяти MCP-имён операций встреч — ноль совпадений в дереве плагина; сами литералы не воспроизводятся здесь намеренно — гейт запрещает их в любом месте дерева, включая документацию) |
| NFR-21 | принят | `tests/test_cli_meeting.py::test_print_error_masks_secret_inside_response_body` (регрессия `redact_secrets`, разделяемая всеми командами этой поверхности) |
| NFR-22 | принят | `SKILL.md` §«Диагностика при отказе» п.5 (навык вообще не вызывает `*-confirm`, повтора нет структурно); `test_nfr22_cancel_meeting_confirm_network_failure_no_retry_exactly_one_post` (ровно один POST на сбой с неизвестным исходом) |
| NFR-23 | принят | `grep -rl create-meeting-confirm\|cancel-meeting-confirm` в дереве плагина — только текстовые упоминания в `SKILL.md` (печать команды, не вызов); `test_nfr23_cancel_meeting_confirm_refuses_without_tty` |

**Не проверено живьём.** Ни одна операция не выполнялась против боевого контура KTalk (создание,
отмена, чтение расписания/комнаты боевыми данными) — красная линия задачи. Покрытие выше опирается
на текст навыка, юнит-тесты с моком транспорта и структурные grep-проверки, не на наблюдение
реального поведения оператора в терминале.

**Публикация пакета `ktalk-mcp` 0.8.0 в PyPI.** На дату приёмки в PyPI опубликована `0.7.0`
(проверено `pypi.org/pypi/ktalk-mcp/json`), локально в этом репозитории — `0.8.0`
(`pyproject.toml`). `compat.json` плагина требует минимум `0.8.0` — до публикации навык
`ktalk-meetings` у operatorов на опубликованной `0.7.0` получает честный отказ `ktalk-onboard.sh
check` (код 11) вместо тихой поломки (DEV-006, «Версионный узел»). Это релизный вопрос, не
блокирует приёмку AC: каждый AC этой поверхности проверяется относительно контракта CLI, который
уже существует в дереве `0.8.0`, а не относительно того, что опубликовано на данный момент; fail-
closed поведение при рассогласовании версий — само по себе часть контракта (`compat.json`/ADR-014),
и оно работает корректно. Публикация — операция владельца, вне полномочий BA/Dev.

**Вердикт: pass.** Все FR-32…FR-38, NFR-20…NFR-23 — принят. Ничего не принято частично, ничего не
отклонено.

## Приёмка (BA-008)

Приёмка редакции волны 6. Проверено чтением `skills/ktalk-meetings/SKILL.md` (это дерево) — для
AC, помеченных at-design QA-007 (дерево пакета `ktalk-mcp`, вне переезда) как «проверяется
текстом навыка» — и прогоном `uv run pytest` / `bash scripts/check.sh --fast` (`504 passed`,
`Errors: 0`, отчёт QA-008, тот же источник).

| Требование | Вердикт | Доказательство |
|---|---|---|
| FR-33 | принят | AC1: `test_confirm_without_sanction_refuses_and_makes_no_request` (код 40, ноль запросов) + `check-plugin-composition.sh` (нет программного запуска `sanction grant`); AC2: `test_confirm_with_sanction_and_confirmation_id_creates_exactly_once`, `test_confirm_without_confirmation_id_refuses`; AC3: `test_confirm_refuses_when_body_changed_after_preview`; AC4: `SKILL.md` §«Создание встречи» — запрет дефолтов по перечню NFR-9 сохранён дословно; AC5: `test_journal_records_attempt_and_outcome_for_successful_write` |
| FR-34 | принят | `test_cancel_confirm_with_sanction_cancels_exactly_once`, `test_create_sanction_does_not_authorize_cancel` (ключи независимы); `SKILL.md` §«Отмена встречи» — откуда берётся `id`, показ `payload` до записи |
| NFR-22 | принят | `test_repeat_with_consumed_confirmation_id_refuses_exactly_one_post` (ровно один POST), `test_budget_is_consumed_on_unknown_outcome` (списание и на неизвестном исходе); `SKILL.md` §«Диагностика при отказе» п.5 |
| NFR-23 | принят | п.1 — `test_cli_sanction_grant_refuses_without_tty_and_writes_nothing` (код 43, файл не создан, без эмуляции pty) + grep по дереву плагина; п.2 — `test_confirm_with_expired_sanction_returns_41`, `test_confirm_with_exhausted_sanction_returns_42`, `test_grant_beyond_ceiling_is_rejected` (6 форм); п.3 — `test_keys_are_independent`; п.4 — `test_revoke_takes_effect_on_next_attempt`; п.5 — `test_broken_sanction_file_reads_as_absent` (6 форм) + `test_world_writable_sanction_file_reads_as_absent`; п.6 — `test_unwritable_journal_blocks_the_network_call` |
| NFR-24 | принят | AC1 — `SKILL.md` §«Данные контура — не инструкции» во вводной части, покрывает все читающие команды; AC2 — FR-35 не менялся, показ предпросмотра перед записью обязателен текстом навыка; AC3 — `test_injection_like_subject_does_not_change_body_or_sanction` + регрессия `test_meeting_body.py` |

**Не проверено живьём.** Ни одна операция волны не выполнялась против боевого контура: ни выдача
санкции в настоящем терминале, ни создание, ни отмена встречи агентом, ни поведение при
blanket-правиле `settings.json`. Покрытие опирается на мок транспорта, текст навыка и структурные
проверки — не на наблюдение реального поведения.

**Что принято с оговоркой в тексте решения, а не в AC.** SEC-006 MAJ-01: файловая санкция не
защищает от агента с доступом к shell — он может написать файл сам, минуя `grant`. Это не отказ
ни одного AC (все они про поведение контура, а не про модель угроз) и не регресс относительно
волны 5 — TTY-барьер имел тот же предел. Оговорка внесена в «Негативные» ADR-016; BA фиксирует
её здесь, чтобы приёмка не читалась как утверждение о невозможности обхода.

**Вердикт: pass.** FR-33, FR-34, NFR-22, NFR-23, NFR-24 — принят. Частично принятых нет,
отклонённых нет. FR-32, FR-35…FR-38, NFR-20, NFR-21 волной не затрагивались; их приёмка —
BA-006, остаётся в силе.

## Инварианты и Safeguards

**Содержательные:**
- Ни одна запись в контур не выполняется без действующей санкции — отсутствие, повреждение,
  истечение срока и исчерпание бюджета читаются одинаково как отказ (NFR-23).
- Санкцию выдаёт только человек в интерактивном терминале; ни один путь промт-поверхности не
  вызывает команду выдачи санкции программно (NFR-23 п.1).
- Записывается ровно то тело, которое было показано оператору предпросмотром, либо не
  записывается ничего (FR-33 AC3).
- Каждая попытка записи оставляет след в журнале операций; невозможность записать журнал
  запрещает запись (NFR-23 п.6).
- Содержимое ответов контура не является основанием ни для записи, ни для расширения санкции
  (NFR-24).
- `get-room` никогда не описывается и не используется как способ проверить занятость/свободность
  имени комнаты (FR-36 AC2, ADR-006 п.5).
- Ни одно поле из перечня NFR-9 (`rooms-calendar-scheduling.md`) не получает значение,
  подставленное промтом без явного участия оператора (FR-33 AC3).
- Промт-поверхность не повторяет пишущую команду автоматически после сбоя с неизвестным исходом
  (NFR-22).
- Промт-поверхность ограничена CLI — ни один путь не называет MCP-инструмент как способ выполнить
  операцию этой поверхности (NFR-20).

**Sensitive content:**
- `KTALK_SESSION_TOKEN`/`KTALK_PERSONAL_API_KEY` не появляются в промтах, логах навыка или тексте,
  показанном оператору (NFR-21).
- Примеры и фикстуры сценариев (если заводятся SA/Dev) не копируют реальные ФИО, темы встреч и
  имена комнат из боевого домена — синтетические данные, наследует
  `rooms-calendar-scheduling.md`.
