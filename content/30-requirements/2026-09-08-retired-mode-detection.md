---
properties:
  - name: Тип контента
    value: [Требование]
  - name: Фаза
    value: [Pilot]
  - name: Статус
    value: [Draft]
  - name: Audience
    value: [Internal]
---

# Обнаружение отставного режима авторизации: `check` называет факт и не устраняет его

**Дата:** 2026-09-08
**Автор:** BA (BA-001, эпик `ktalk-plugin-swm`)
**Входы:** `content/30-requirements/2026-09-07-session-only-auth.md` (BA-001 предыдущего эпика —
решение об одном режиме); `content/00-project/adr/ADR-028-session-only-authorisation.md` (Д1 —
граница «плагин не читает значение и не выбирает режим», Д3 — какие операции недостижимы под
единственным режимом); `content/10-domain/research/2026-09-07-cli-2-1-0-auth-modes.md` (RES-001
§1 — приоритет источников токена, включая `.env` как третий по приоритету источник ниже
переменных окружения; §2 — таблица `OPERATION_PROFILES`, пять операций с профилем только
`SESSION`); `openspec/specs/plugin-onboarding-sanctioned-install/spec.md` (Requirement «The
authorisation instruction never carries a secret value» — действующий запрет печати значения;
Requirement «Absence of the CLI is detected before the first circuit operation» — точка вызова
`check`); `scripts/ktalk-onboard.sh:170-183` (`report()` — форма ответа `check --json`),
`scripts/ktalk-onboard.sh:311` (`run_clean` — единственное сегодняшнее упоминание
`KTALK_PERSONAL_API_KEY` в скрипте, внутри `unset` чистого подпроцесса, не в проверке);
`references/onboarding.md` (структура «Code N — …» и прецедент кода 11: «a version mismatch is
a warning, not a blocker»); заявка `github.com/mdemyanov/ktalk-cli#10` (пакет 2.1.0 продолжает
читать личный ключ — устранение переменной сегодня не мигрирует ничего); решение владельца от
2026-09-08 не заводить удаляющий хук (обоснование — ADR-022: плагин не объявляет вторую
интерфейсную поверхность; модель санкции: команду, меняющую состояние машины, запускает человек
в своём терминале, `plugin-onboarding-sanctioned-install` Requirement «Automatic install and
update each require their own explicit sanction»).
**Потребители:** SA (место вставки проверки в `cmd_check`, точный текст нового раздела «Code 14»
в `references/onboarding.md`), Dev, QA-author.
**Capability:** `openspec/specs/session-only-auth/spec.md`

## JTBD

Когда я как оператор прохожу автоматическую проверку `ktalk-onboard.sh check` перед первой
операцией сессии, а переменная `KTALK_PERSONAL_API_KEY` всё ещё стоит в моём окружении с
прежних времён (до ADR-028), я хочу, чтобы плагин прямо назвал этот факт, назвал команду,
снимающую переменную в текущей оболочке, и назвал операции, которые из-за неё откажут, — чтобы
я не тратил время на разбор непонятного отказа `get-room`/`list-calendar`/`create-meeting`/
`cancel-meeting`/`search-contacts` при внешне исправном токене сессии, и чтобы я сам решил, стоит
ли и как убирать переменную из моей оболочки на постоянной основе — это моё решение и мои файлы,
не плагина.

**Discovery path.** Автоматический вызов `scripts/ktalk-onboard.sh check` (и `check --json`)
агентом перед первой операцией контура за сессию (`plugin-onboarding-sanctioned-install`,
Requirement «Absence of the CLI is detected before the first circuit operation») — оператор не
идёт искать этот текст сам, агент показывает его как часть обычного онбординг-цикла; второй вход
— новый раздел «Code 14» `references/onboarding.md`, тот же файл и тот же принцип «читается,
когда `check` вернул ненулевой код», что уже устанавливают разделы «Code 10»–«Code 13».

## Описание

Предмет — узкое дополнение к `scripts/ktalk-onboard.sh check`: обнаружить, что переменная
`KTALK_PERSONAL_API_KEY` задана в окружении процесса, назвать этот факт (никогда не значение) в
`--json` и в тексте, назвать команду немедленного действия и операции, которые от находки
страдают. Устранение переменной, правка файлов оболочки, миграция кода пакета — вне периметра;
это решение владельца зафиксировано отдельно и этим требованием не пересматривается (см. «Входы»).

Обнаружение ограничено окружением процесса, в котором выполняется сам `check`. Оно не читает
`~/.zshenv` и любые другие файлы автозапуска оболочки, не читает файл `.env` рабочего каталога —
оба класса файлов оставлены оператору по одному основанию (обоснование — ниже). `check`
по-прежнему не читает значение переменной ни при каком исходе — этот запрет уже действует
(`plugin-onboarding-sanctioned-install`, Requirement «The authorisation instruction never
carries a secret value») и не ослабляется дельтой этого требования, только дополняется новым
именем факта.

**Не входит в периметр:** снятие поддержки личного ключа в коде `ktalk-cli` (заявка `#10`);
правка `~/.zshenv` или любого другого файла оболочки — плагином или по его подсказке; девять
`### Requirement:` capability `session-only-auth`, принятые прошлым эпиком, — этим требованием
не пересматриваются, только дополняются тремя новыми.

## Почему предупреждение, а не блокировка проверки

Личный ключ, заданный поверх исправного токена сессии, не ломает контур целиком: `ktalk-cli`
2.1.0 отдаёт ключу приоритет только там, где у операции вообще нет профиля `API_KEY`, — и это
ровно пять операций, зеркально названных RES-001 §2 (`get-room`, `list-calendar`,
`create-meeting`, `cancel-meeting`, `search-contacts`). Всё остальное, чем оператор
пользуется чаще — `list-recordings`, `get-transcript`, `get-summary`, `get-conference`,
`get-participants` и их CLI-обвязки, — под личным ключом продолжает работать: RES-001 §3
относит их к операциям, «которые вообще не идут через `OPERATION_PROFILES`» или имеют
профиль под обоими режимами, не только под сессией. Считать находку тем же классом, что
отсутствие пакета (`missing_cli`) или неверная версия (`outdated`, тоже прецедент
предупреждения, не блокера, — см. ниже), было бы неверно уже потому, что там ломается всё, а
здесь — пять конкретных команд из одного навыка. Останавливать всю сессию проверкой `check` на
находке, которая не мешает большей части контура, было бы дороже, чем полезнее: агент,
которому предстоит только читать записи, не должен упираться в отказ проверки из-за
переменной, не имеющей отношения к его задаче.

Текст `references/onboarding.md` уже устанавливает прецедент именно такого класса для кода
11 («Work can continue: a version mismatch is a warning, not a blocker. Some scenarios may not
work.») — формулировка дословно переносится на находку этого требования: работа может
продолжаться, часть сценариев — нет, и агент обязан не прерывать сессию по одной этой находке,
а показать её оператору и продолжить.

## Почему обнаружение — только переменная процесса, не файлы

RES-001 §1 называет три источника токена по приоритету: переменная окружения (или `.env` в
cwd, который pydantic-settings читает ниже переменной, но выше файла токена) → файл токена.
Чтение `~/.zshenv` и чтение `.env` рабочего каталога — разные файлы, но одно и то же
превышение полномочия: оба требуют читать содержимое файла, которым не управляет и не должен
управлять `ktalk-onboard.sh check`, тогда как переменная окружения процесса — это ровно то
состояние, в котором `check` уже читает `PATH` и версию установленного пакета (`installed_
identity`, `pin_version`) для диагностики без записи. Разница по существу: значение переменной,
заданной в текущей оболочке, действует на CLI из той же самой сессии, в которой работает
агент, — обнаружение здесь предсказывает реальный ближайший отказ. Значение, лежащее только в
`~/.zshenv` или в `.env`, ещё не подействовало на текущую сессию (если не было явно
подгружено) и подействует только после нового захода в оболочку либо после `source` — искать
его сейчас означало бы предупреждать о риске, который в этой сессии, возможно, никогда не
реализуется, ценой чтения файла, для которого у `check` нет отдельного полномочия.

Второе основание — перечислимость. У переменной окружения процесса ровно один источник
истины — сама переменная `KTALK_PERSONAL_API_KEY`, проверяемая одной командой (`[ -n
"$KTALK_PERSONAL_API_KEY" ]`, без чтения значения дальше факта непустоты). У файлов автозапуска
оболочки таких источников много и по-разному для разных оболочек (`~/.zshenv`, `~/.zshrc`,
`~/.bashrc`, `~/.bash_profile`, `~/.profile`, и это не полный список) — попытка проверить их
все создаёт риск ложноотрицательного результата (не проверили нужный файл) без гарантии
устранить его чтением ещё большего числа файлов. Оператор, а не `check`, знает, какую оболочку
он использует и что в её файлы записано, — то же разделение полномочий, что уже действует для
устранения (эпик прямо запрещает предписывать правку dotfiles плагином).

## Почему код возврата 14

Свободные номера: 14, 15, 21, 35 и далее. Выбран 14 — по смежности с семейством исходов,
которые уже эмитирует `cmd_check` (10 `missing_cli`, 11 `outdated`, 12 `missing_uv`, 13
`wrong_package`): находка этого требования — ещё один исход того же `cmd_check`, не
внутренняя ошибка (`20`, отдельный класс — «`compat.json` не прочитан») и не относится к
подсистеме санкции на установку (`30`–`34` — `grant`/`revoke`/`install`, другая команда
скрипта). Держать исходы одной и той же команды `check` в одном контигентном диапазоне
номеров облегчает чтение `references/onboarding.md`, где разделы уже идут по порядку «Code
10» → «Code 13»: новый раздел «Code 14» продолжает тот же ряд, не начинает новый.

## Инварианты и защитные ограничения

**По существу:**
- `check` (текст и `--json`) никогда не печатает значение `KTALK_PERSONAL_API_KEY` — только
  факт, задана ли она в окружении процесса.
- Обнаружение читает исключительно окружение процесса, в котором выполняется сам `check`; оно
  не читает `~/.zshenv`, любой другой файл автозапуска оболочки и файл `.env` рабочего
  каталога.
- Находка не заменяет и не предшествует проверке готовности пакета (коды 10–13, 20, 30–34):
  она сообщается отдельным кодом 14 только когда версия пакета и так совпадает с пином.
  Наличие находки не мешает `check` вернуть коды 10–13/20/30–34, когда для них есть основание —
  готовность пакета остаётся приоритетнее.
- `check` никогда не выполняет `unset`, не правит ни один файл оболочки и не предлагает
  сделать это за оператора.
- Сообщение называет точную команду немедленного действия (`unset KTALK_PERSONAL_API_KEY`) и
  явно перечисляет пять операций, которые откажут (`get-room`, `list-calendar`,
  `create-meeting`, `cancel-meeting`, `search-contacts`) — не обобщённое предупреждение без
  конкретики.

**Чувствительный контент:** N/A — требование описывает факт присутствия переменной и текст
диагностики, не приводит и не подразумевает значение токена, личного ключа или иных ПДн.

## Ломающее изменение

Раздел обязателен условием приёмки; `N/A` недопустим. В репозитории нет `CHANGELOG.md` и нет
`scripts/check-breaking-change-section.py` — автоматический гейт неприменим, разбор — вручную,
по образцу «Симптом → Причина → Починка → Что не изменилось», уже принятому в этом дереве
(`content/30-requirements/2026-09-07-session-only-auth.md`, раздел «Ломающее изменение»).

**Кандидат — новый ненулевой код возврата (14) у `cmd_check`.**

- *Симптом.* Вызывающая сторона, которая сегодня трактует любой ненулевой код `check` как
  «пакет не готов, не продолжать» (без специального разбора кода 11, аналогично тому, как это
  уже требуется для предупреждения о несовпадении версии), после этой правки получит тот же
  грубый отказ на сессии, где пакет полностью исправен и подавляющее большинство операций
  контура работает без проблем — пострадает готовность, которой ничто не угрожало.
- *Причина.* Решение назвать находку отдельным предупреждающим кодом (обоснование — «Почему
  предупреждение, а не блокировка» выше), а не молчаливым дополнительным полем внутри исхода
  `ok`: машинный потребитель обязан различать исходы по коду/полю, не по тексту сообщения — тот
  же принцип, что уже действует для `meetings-prompt-surface-reads` (различение по коду
  возврата, не по разбору текста).
- *Починка.* Вызывающая сторона (промт-слой этого же плагина — `agents/ktalk-processor.md`,
  `agents/ktalk-evaluator.md`, любой навык, вызывающий `check` перед первой операцией контура)
  обязана обработать код 14 так же, как уже обязана обрабатывать код 11: показать находку
  оператору и продолжить сессию, не прерывать её. Это задача SA/Dev следующего шага, не этого
  требования — здесь фиксируется факт цены, не её устранение.
- *Что не изменилось.* Для оператора без переменной `KTALK_PERSONAL_API_KEY` в окружении
  поведение и коды `check` не меняются ни на бит — код 14 никогда не возвращается. Коды 10,
  11, 12, 13, 20, 30, 31, 32, 33, 34 сохраняют точно те же значения и то же значение, что и до
  этой правки; ни один из них не переиспользуется и не переименовывается.

**Кандидат 2 — новое поле в `check --json` (не ломающее, для полноты сверки).** Форма ответа
`report()` (`scripts/ktalk-onboard.sh:170-183`) уже добавляла поля аддитивно без разрыва
совместимости (`installed_package`/`pinned_package`, ADR-024 companion, «Consequences») —
новое булево поле факта этого требования следует тому же прецеденту: потребитель, читающий
`--json` по именованным ключам, не теряет ни одного уже существующего поля и получает
дополнительный ключ, который ранее не мог ожидать, но и не мог полагаться на его отсутствие
как на контракт.

## Приёмка (BA-001)

Приёмка эпика `ktalk-plugin-swm` (`main...HEAD`, 11 коммитов, 16 файлов, +1268/−12, HEAD
`6e8e2c7`) против трёх новых `### Requirement:` (строки 155–227
`openspec/specs/session-only-auth/spec.md`), их 6 `#### Scenario:`. Проверено чтением дерева на
`HEAD` и живыми прогонами этой задачей (`bash scripts/test-retired-mode-detection.sh`, два прямых
вызова `scripts/ktalk-onboard.sh check --json` с переменной и без, `grep` по всему дереву,
`bash scripts/check.sh --full`, `npx -y @fission-ai/openspec@1.8.0 validate --specs --strict`) —
не по самоотчёту QA-runner (`test-reports/006-2026-09-08.md`), там, где он цитируется, рядом
стоит собственная проверка того же факта.

| Requirement / Scenario | Вердикт | Доказательство |
|---|---|---|
| `check` detects the retired mode in the process environment only, never in a file — «The fact is named on every outcome, the value never is» | принят | Собственный запуск с именованной фикстурой (`FIXTURE_VALUE="…"; KTALK_PERSONAL_API_KEY="$FIXTURE_VALUE" bash scripts/ktalk-onboard.sh check --json`) → `"retired_mode_detected":true`, `"status":"retired_mode"`, значение фикстуры нигде в выводе; `env -u KTALK_PERSONAL_API_KEY bash scripts/ktalk-onboard.sh check --json` → `"retired_mode_detected":false`, `"status":"ok"` |
| То же Requirement — «A variable not yet exported into the running session is not reported» | принят | Чтение `scripts/ktalk-onboard.sh:211-213` (`retired_mode_set()` — ровно `[ -n "${KTALK_PERSONAL_API_KEY-}" ]` внутри процесса `check`); `grep -n "zshenv\|bashrc\|profile\|\.env" scripts/ktalk-onboard.sh` — совпадения только в комментариях, описывающих запрет, ни одного вызова чтения такого файла в теле `cmd_check`/`retired_mode_set` |
| Detecting the retired mode is a warning, not a package-readiness failure — «A correctly installed package with the retired mode set is neither ok nor a package error» | принят | Тот же живой вызов выше — код возврата 14 (`echo $?`), не 0 и не 10–13/20/30–34; чтение `scripts/ktalk-onboard.sh:24-29` — `E_RETIRED_MODE=14`, отдельная константа рядом с `E_MISSING_CLI`…`E_WRONG_PACKAGE`, не переиспользует ни одну |
| The reported message names the affected operations and the actual remedy — «The message names the affected commands, not a generic warning» | принят | Живой вывод выше — текст `message` называет явно все пять операций (`get-room, list-calendar, create-meeting, cancel-meeting, search-contacts`) |
| То же Requirement — «The message names the immediate command and does not claim it is permanent» | принят | Тот же вывод — «Немедленное действие в этой оболочке: unset KTALK_PERSONAL_API_KEY… не переживёт новую оболочку или новый запуск; то же присваивание в файле автозапуска оболочки или в .env рабочего каталога… останется как было» |
| То же Requirement — «The plugin does not name a specific file or edit one» | принят | Тот же вывод — «check не читает такие файлы, поэтому не знает и не называет, где именно такое присваивание могло бы быть, и не изменяет ни один такой файл»; ни один конкретный путь (`~/.zshenv` и т.п.) не назван |

Собственный прогон этой задачей: `bash scripts/test-retired-mode-detection.sh` → `ИТОГО:
PASS=24 FAIL=0 SKIP=0` (совпадает с QA-002, `test-reports/006-2026-09-08.md`); `bash
scripts/check.sh --full` → `EXIT=0`, `check.sh --full — passed`, новая сьюта реально исполняется
внутри (`✓ scripts/test-retired-mode-detection.sh` в логе прогона, подключена в
`.nauta-gates.yaml` → `projectGates.full`, обязательное действие Dev из `at-design.md`
исполнено); `npx -y @fission-ai/openspec@1.8.0 validate --specs --strict` → `12 passed, 0
failed`, включая `spec/session-only-auth`. `git status --short` на `HEAD` — пусто.

**Вердикт по трём Requirement: pass.** Все три `### Requirement:` и их 6 `#### Scenario:` —
приняты безусловно, ничего не принято частично.

### Красная линия ADR-029 (Д1/Д2) — обнаружение без устранения

Проверено по коду `scripts/ktalk-onboard.sh`, не по тексту раздела «Code 14»:

- **`check` не зовёт `unset` в оболочке оператора и не предлагает это за него.** `grep -n
  "unset" scripts/ktalk-onboard.sh` — единственное совпадение (`:364`, `run_clean`) внутри
  подпроцесса-`(...)` очистки секретов перед вызовом менеджера пакетов, не тронуто этим диффом
  (`git diff main...HEAD -- scripts/ktalk-onboard.sh | grep run_clean` — пусто) и не относится к
  `cmd_check`/`retired_mode_set`; ни `retired_mode_message()`, ни ветка кода 14 `cmd_check` не
  выполняют `unset` — они только форматируют строку с текстом команды.
- **Не читает `~/.zshenv`, `.env` рабочего каталога и другие файлы.** `retired_mode_set()`
  (строки 211-213) — единственная функция, читающая факт; тело — ровно `[ -n
  "${KTALK_PERSONAL_API_KEY-}" ]`, без единого обращения к файлу. Живой AC-2b
  (`test-retired-mode-detection.sh`) подтверждает: переменная, лежащая только в `.env` рабочего
  каталога вызова, не засчитывается.
- **Не называет конкретный файл.** `retired_mode_message()` (строки 230-234) — текст говорит
  «файл автозапуска оболочки» и «.env рабочего каталога» классами, ни одного конкретного пути
  (`~/.zshenv`, `~/.bashrc` и т.п.) нет ни в самой функции, ни в её живом выводе (проверено выше).
- **Ничего не правит.** `git diff main...HEAD --stat` — ни один файл вне дерева плагина не
  затронут по построению (скрипт запускается в дереве плагина и не открывает файлы операторской
  машины на запись); в самом `cmd_check`/`retired_mode_*` нет ни одного вызова с перенаправлением
  `>`/`>>`/`sed -i` на что-либо, кроме печати в stdout.

Ни одного перехода решения ADR-029 в реализации не найдено.

### Ломающее изменение — все вызывающие стороны, не только две названные

Раздел обязателен условием приёмки, `N/A` недопустим. В репозитории нет `CHANGELOG.md` и нет
`scripts/check-breaking-change-section.py` — автоматический гейт неприменим, разбор — вручную.

Требование называет кандидатом новый ненулевой код 14 у `check` и поручает Brief for SA обработку
в `agents/ktalk-processor.md`/`agents/ktalk-evaluator.md`. Проверка нашла **пять** вызывающих
`scripts/ktalk-onboard.sh check --json` перед первой операцией контура (`grep -rn
"ktalk-onboard.sh check" agents/ skills/`), не две:

| Вызывающий | Обработка кода 14 | Достаточно? |
|---|---|---|
| `agents/ktalk-processor.md:33` | «Exit code 0 — carry on. A non-zero code — read `references/onboarding.md` and follow it» | да — не хардкодит собственную интерпретацию, делегирует `onboarding.md` |
| `agents/ktalk-evaluator.md:26` | тот же дословный текст | да, тем же основанием |
| `skills/ktalk-meetings/SKILL.md:22` | тот же дословный текст | да, тем же основанием |
| `skills/ktalk-eval/SKILL.md:21` | тот же дословный текст | да, тем же основанием |
| `skills/ktalk-registry/SKILL.md:23` | тот же дословный текст | да, тем же основанием |

Все пять — не два, названные требованием, — используют идентичную формулу диспетчеризации без
собственного разбора кода: ни один не трактует «любой ненулевой» как «пакет не готов, не
продолжать» напрямую в своём тексте, все перекладывают трактовку на
`references/onboarding.md`. Этот файл действительно несёт новый раздел «## Code 14 — the retired
authorisation mode is set» (строки 89–113) с формулировкой «work can continue… the same class
already established for code 11», дословно продолжающей прецедент кода 11. Значит вопрос
«достаточно ли раздела в `onboarding.md`, или где-то остался вызывающий, для которого 14 значит
„пакет не готов“» закрывается одним разделом на все пять сегодняшних мест вызова — расхождения
нет ни в одном.

`README.md:219` тоже упоминает `ktalk-onboard.sh check`, но это инструкция для человека-оператора
в разделе «Если что-то не работает» — не автоматический диспетчер с собственной интерпретацией
кода; оператор читает текст находки напрямую, без пере-классификации кодом. Не тот класс
вызывающего, не требует правки.

Версия плагина поднята один раз: `git log --oneline main..HEAD -- .claude-plugin/plugin.json` —
один коммит (`847e35c`, DEV-001), `1.15.0` → `1.16.0`, minor, соответствует `CONTRIBUTING.md`
(«Версия плагина и тег релиза») — правка того же коммита меняет `references/onboarding.md`.
Формальный периметр сторожа `check_prompt_version_sync` (`agents/`, `skills/ktalk-registry/`,
`references/ktalk-processor/`) этот файл не покрывает буквально (сам сторож проверено —
`prompt_diff` по этим трём путям в этом диффе пуст), но бюст версии сделан проактивно и не
противоречит правилу: гейт не запрещает поднимать minor там, где сам не требует, только не
допускает пропуск там, где требует. Второго подъёма нет (`grep -c '"version"' git log -p
main..HEAD -- .claude-plugin/plugin.json` — одно изменение).

### Гейты акцептанс-режима

| Гейт | Статус | Комментарий |
|---|---|---|
| Backlog-closure | ✓ n/a | `scripts/check-backlog-closure.py` в дереве нет |
| Breaking-change-guide | ✓ n/a | `scripts/check-breaking-change-section.py` и `CHANGELOG.md` в дереве нет; содержательный разбор — раздел «Ломающее изменение» выше, расхождений не найдено, все пять вызывающих закрыты одним разделом `onboarding.md` |
| Downstream-test-touch | N/A | в репозитории нет `roadmap.md` и учёта закрытых эпиков; правка `scripts/test-onboard.sh` в этом диффе — перебазировка снимка sha256 своего же эпика (DEV-001), не тест чужого закрытого эпика |

### Прошлый эпик и периметр — сверка

Находки 1 и 3 аудита `2026-09-08-session-only-auth-security-audit.md` в периметр этого эпика не
входят и не переоткрыты: `git diff main...HEAD -- scripts/check-plugin-composition.sh
openspec/specs/plugin-onboarding-sanctioned-install/spec.md` — пусто, реализация этого эпика их
не задевает. Находка 2 того же аудита (зелёная сьюта существует, но не подключена к гейту) была
явно поименована в `at-design.md` как «ОБЯЗАТЕЛЬНОЕ действие Dev» и закрыта тем же приёмом:
`scripts/test-retired-mode-detection.sh` добавлена в `.nauta-gates.yaml` →
`projectGates.full` (проверено выше, живой прогон `check.sh --full` исполняет сьюту) — та же
находка не повторена в этом эпике.

Ничего сверх согласованного периметра (три `### Requirement:`, шесть `#### Scenario:`, ADR-029,
`references/onboarding.md`, `.nauta-gates.yaml`, версия плагина) не найдено:
`git diff main...HEAD --stat` — 16 файлов, все объяснены выше или тривиальны (`_index.md`
каталогов, `.nauta-ids.yaml` highwater под выданный ADR-029). Ни один файл вне названного
периметра не тронут.

## Brief for SA

**Требование:** этот файл
**Capability:** `openspec/specs/session-only-auth/spec.md`
**Фаза:** Pilot

**Спроектировать:** место вставки проверки `KTALK_PERSONAL_API_KEY` внутри `cmd_check`
(после совпадения версии с пином, до `report ok`); имя нового булева JSON-поля и его позиция в
`report()`; точный текст нового раздела «Code 14» `references/onboarding.md` по образцу разделов
«Code 10»–«Code 13»; обработку кода 14 в `agents/ktalk-processor.md`, `agents/ktalk-evaluator.md`
и любых навыках, вызывающих `check` перед первой операцией контура (не прерывать сессию,
показать находку).

**Бизнес-правила для валидаций:** «`check` detects the retired mode in the process environment
only, never in a file», «Detecting the retired mode is a warning, not a package-readiness
failure», «The reported message names the affected operations and the actual remedy» —
`openspec/specs/session-only-auth/spec.md`.

**Критерии приёмки архитектуры:** все `#### Scenario:` перечисленных требований той же спеки.
